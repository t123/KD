@using KD.Infrastructure.ViewModels.Properties
<MudStack>
    @for(int i=0;i<50;i++)
    {
        <MudNavLink>XXXXXX</MudNavLink>
    }
</MudStack>

@code {
    [Parameter]
    public required PropertyViewState State { get; set; }

    [Parameter]
    public required PodPropertyViewModel? ViewModel { get; set; }
}